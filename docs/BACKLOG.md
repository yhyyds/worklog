# Worklog 本地迁移与后续待办

## P0：本机接管门禁

- [x] 将仓库完整镜像为 bare mirror，并生成 `--all` Git Bundle。
- [ ] 导出或归档 GitHub PR、Issue、Release、Actions 产物和原始 ChatGPT 会话。
- [ ] 从托盘彻底退出 Worklog。
- [ ] 备份实际数据库目录、`storage-location.json` 所在目录和完整 Obsidian Vault。
- [ ] 为备份生成 SHA-256 文件清单，并在另一存储介质保留副本。
- [x] 在本机签出 `codex/0.9.1-local-handoff`，确认祖先包含 `7d2fdfc`。
- [x] 生成并提交 `package-lock.json`、`src-tauri/Cargo.lock`。
- [x] 记录 `node --version`、`rustc --version`、`cargo --version`。
- [ ] 运行前端、Rust、桌面启动和 Windows 安装包基线验证（前端、Rust 与双安装包已完成，桌面启动待验证）。

## P1：基于 0.9.1 的下一轮功能

- [x] 删除 `src-tauri/src/obsidian.rs` 日记渲染中的 `<details>` 与 `<summary>`。
- [x] 每轮专注输出一条父级列表记录。
- [x] 每个时间轴事件紧随父行，并缩进一个制表位。
- [x] 父行与首个子事件之间不插入空白行。
- [x] 添加精确字符串回归测试，覆盖多轮专注、任务切换、暂停原因和非专注记录。
- [x] 新增不内置 WebView2 的 Tauri 配置。
- [x] 新增脚本一次构建两份 NSIS 安装包。
- [x] 为两份安装包使用明确文件名并生成 SHA-256，且已完成本机实包验证。
- [x] 更新 README、CHANGELOG 和发行说明。
- [x] Windows CI 配置为真实构建并上传两份安装包。

## P2：后续发行完善

- [ ] 配置 Windows 商业代码签名证书，降低 SmartScreen 提示。
- [ ] 为依赖和工具链建立定期升级流程，不在功能迭代中隐式升级。
- [ ] 建立数据库备份恢复演练与版本升级回滚说明。

## 1.0：个人成长系统

- [x] 新建本机分支 `codex/1.0.0-personal-growth-system`，不合并远端既有 `0.9.2`。
- [x] 将关闭策略移至 Rust，并在设置中支持退出或隐藏到托盘。
- [x] 增加历史日期查看、日记同步状态、文件占用重试和指定日期重新保存。
- [x] 增加倒计时/正向计时模式和暂停时间持久化。
- [x] 增加昨日打卡、依赖有效性和不可重复结算。
- [x] 增加长期目标、阶段随想、计划次数、必做/附加和奖杯进度。
- [x] 增加规则周报、历史基准、上周对比和 PNG 导出。
- [ ] 将古典引语从 40 条扩充至至少 400 条，并完成逐条出处审校（当前不能称为已核验）。
- [x] 待办箱无日期收集、现有任务收纳、父子任务组排期及永久 ID 保留。
- [ ] 长期目标按日发生、顺序依赖、一次性顺延及真实周期，替换目前次数计数器。
- [x] 增加 debug-only `--demo`，临时数据库和测试 Vault 与正式数据隔离。
- [x] 按最新要求取消每轮完整 demo 门禁，保留自动化测试与构建，用户负责试用反馈。
- [x] 周报增加习惯周历、连续/最长天数、中断次数、专注明细、任务清单与近四周同期对比；清理界面技术宣言和空泛文案。
- [ ] 完成真实桌面交互冒烟与两份 1.0.0 Windows 安装包构建。

## 1.1：分类规划与隐私分享

- [x] 本机新分支 `codex/1.1.0-private-category-planning`，版本同步为 1.1.0。
- [x] 长期目标任务编辑、删除、每日排期与独立重要/紧急设置。
- [x] 每日执行同步目标进度，一次性顺延保留关联，重复任务禁止顺延。
- [x] 自定义彩色分类；周报分类打卡与目标完成情况。
- [x] 分类公开/匿名/排除与名称单独授权；先过滤再统计、独立分享图片与导出预览。
- [x] 幂等向前迁移与隐私、日程、导出回归测试。
- [x] 构建并校验两份 1.1.0 Windows 安装包（18 项前端测试、63 项 Rust 测试通过，安装包 SHA-256 已核对）。

## 后续完成定义

- 基线明确来自 `0.9.1`，不包含现有 `0.9.2` 提交。
- 前端和 Rust 测试全部通过。
- 在真实 Windows runner 上成功产生两份安装包。
- Markdown 文件中不再出现 `<details>` 或 `<summary>`。
- Obsidian 可通过嵌套列表折叠时间轴。
- 测试未写入或覆盖真实数据库与真实 Vault。
- PR 中说明产物文件名、大小、SHA-256 和 WebView2 差异。
