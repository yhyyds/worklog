# Changelog

Worklog 遵循语义化版本。

## [1.2.0] - 2026-09-07

### Added

- “待办箱”升级为“待办与历史”，侧栏进入时默认展示今天以前仍未完成、且尚未被重新安排的任务。
- 历史任务显示原日期、原编号、状态及仍未完成的子任务，可选择今天或未来日期重新安排。
- 父任务重新安排时只携带未完成子任务；永久任务 UUID、原日期任务实例和不可变事件全部保留，新日期重新编号。
- 重复目标任务保留在历史视图中，但必须回到“成长”调整日期，避免绕过目标计划次数。

### Data

- 不新增迁移或删除记录；重新安排复用 `carry_from_instance_id` 保存来源链路，并在单一事务中写入新实例及来源/目标事件。

## [1.1.0] - 2026-09-06

### Added

- 长期目标任务可编辑内容、类型、必做/附加、计划次数、重要程度与紧急程度，支持选择多个执行日期及批量安排阶段内每天/工作日。
- 日期安排成为“我的一天”真实任务，完成和撤销完成同步到目标；一次性任务顺延保留关联，重复任务及其子任务不能顺延或收入待办箱。
- 删除计划使用软删除，移除今天及未来尚未开始的安排，保留历史任务 UUID、执行记录与不可变事件。
- 打卡与长期目标共用可自建、改名、换色的分类；阶段及每日目标任务继承分类。
- 周报加入分类打卡完成率、待回顾、最长连续、中断次数及目标必做/附加完成情况。
- 分享分类支持公开、匿名汇总、不参与分享；具体事项名称默认不公开，只有公开分类下明确勾选才展示。
- 独立分享数据投影先排除私密内容，再计算每日、历史对比、专注与引句；分享渲染器不接触个人详细周报。
- 隐私分享偏好保存于本地；分享图和个人图均可整图预览后保存 PNG。
- 托盘常驻跨午夜后自动刷新今日日期；进行中的专注/休息仍留在原日期，结束后切换。

### Data

- 新增幂等迁移 `0009_planning_privacy.sql`，不删除旧任务、事件、手动完成次数或用户文件。
- 不改动已发生安排的日期与执行标题；既有手动次数无日期，保留在目标累计进度，不推断到周报。

## [1.0.0] - Unreleased

### Added

- 周报新增每个习惯的七日记录、连续完成、本周/历史最长、中断次数及上周同期；区分未完成、前置项未完成、待回顾和未到日期。
- 周报新增专注轮次、每轮平均/最长时间、暂停/任务切换次数、每日明细、四象限任务分布、完成/未完成清单和近四周同期表格。
- 图片导出同步包含新增明细，按内容计算高度并自动换行；导出入口移至顶部。
- 全界面清理冗余宣传、实现说明和生硬术语。取消每轮完整 demo 门禁，改为自动化测试、编译及用户试用反馈。

- 待办箱：无日期任务收集、现有任务移入及重新排期；保留父子结构、永久 ID、完成状态和原始事件。
- 仅调试版支持可选的 `--demo` 隔离演示模式，内置近四周合成计划、专注、依赖打卡和目标数据。
- 周报统计改为上周同期对比，未回顾打卡单列；历史日记和周报日期切换增加过期请求防护。

- 新增昨日打卡复盘、依赖关系和“原始完成/有效完成”双重结果；重复打卡不顺延，结算结果写入今日时间线。
- 新增长期目标、自定义周期、最近阶段随想、必须/附加与重复/一次性计划，以及可超过 100% 的奖杯进度。
- 新增“一周小笺”，以历史中位数与上周数据生成规则化叙事、离线引语、七日节奏和 PNG 长图。
- 番茄钟新增正向计时模式，暂停时间不计入专注时长。
- 历史日记面板可查看任意日期的完成情况、同步状态和 Markdown 预览，并重新保存指定日期。
- 设置页新增关闭窗口策略，可选择直接退出或隐藏到托盘。

### Fixed

- 主窗口右上角关闭按钮不再被硬编码为隐藏到托盘。
- Obsidian 或插件临时占用日记文件时会自动重试；仍失败时保留错误状态和备份，日后可重试而不伪装成功。

### Data

- 新增仅向前的 `0006_focus_modes.sql` 与 `0007_growth_system.sql`；现有任务、事件、日记设置和用户文件不会被覆盖或删除。

## [0.9.2] - 2026-09-05

### Added

- 提供统一脚本，一次构建 `no-webview2` 与 `with-webview2` 两份 Windows NSIS 安装包并生成 `SHA256SUMS.txt`。
- CI 与 Release 使用锁定依赖，真实构建、校验并上传两份安装包。

### Changed

- Obsidian 每轮专注改用父级 Markdown 列表和制表位缩进的子事件，依靠原生嵌套列表折叠，不再输出 `<details>` 或 `<summary>`。

## [0.9.1] - 2026-09-03

### Fixed

- 父任务和子任务的完成标记改用固定 viewBox SVG，消除字体基线和原生按钮偏移导致的勾选图标不居中。
- 为完成按钮补充可访问名称和按下状态，并增加回归测试。

## [0.9.0] - 2026-09-03

### Added

- 父任务、子任务标题及当天计划时间可编辑。
- 字号支持 85%–130% 调整。
- 四象限独立滚动、快捷新增和完成项置底。
- 暂停专注必须填写原因；专注只允许选择一级任务；切换任务写入可见时间线。
- 每日日记按番茄钟轮次生成可折叠记录。

### Changed

- “我的一天”始终作为主页面，随笔、Obsidian、设置只打开弹窗。
- 番茄钟时长保存后，空闲计时器立即刷新。

## [0.8.0] - 2026-09-03

### Added

- 集中设置番茄钟、本地 SQLite 数据目录、Obsidian 工作区和日记根目录。
- 本地数据库目录支持安全迁移、验证和即时切换，原库保留。

### Changed

- 左侧导航收敛为“我的一天、随笔、Obsidian、设置”。

## [0.7.1] - 2026-09-02

### Fixed

- 为左侧六个导航按钮补齐导航状态与交互，接通专注、工作想法、随笔、Obsidian 和设置界面。
- 移除日记路径中固定的“工作日志”目录，允许在 Obsidian 工作区内选择日记根目录。
- 拒绝工作区外、父目录穿越和符号链接越界的日记根目录。

## [0.7.0] - 2026-09-02

### Added

- Windows NSIS 正式安装包与 GitHub Release 自动发行。
- WebView2 离线安装运行时、简体中文/英文安装界面。
- 正式应用图标与 SHA-256 下载校验文件。
- PR 安装包冒烟构建和三处版本一致性检查。

### Changed

- 安装模式固定为当前用户，不要求管理员权限。
- README 增加下载、安装、构建和签名限制说明。

## [0.6.0] - 2026-09-02

- 完整番茄钟与长短休息生命周期。
- 后台绝对时间校正、系统通知、托盘和全局快捷键。

## [0.5.0] - 2026-09-02

- Obsidian Markdown 笔记浏览、搜索、编辑与预览。

## [0.4.0] - 2026-09-02

- 日终收尾、选择性顺延、次日重新编号和永久 ID 保留。

## [0.3.0] - 2026-09-02

- Obsidian 每日日记安全同步、受管理区块、备份与冲突保护。

## [0.2.0] - 2026-09-02

- 四象限任务、事件时间线、工作想法和基础专注计时。

## [0.1.0] - 2026-09-02

- 产品、架构、数据模型与 Windows 本地优先工程骨架。
